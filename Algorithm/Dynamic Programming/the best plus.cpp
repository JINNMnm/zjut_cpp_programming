#include <iostream>
#include <string>
#include <iomanip>
#include <cmath>
#include <algorithm>
using namespace std;
int Num(string a){
    int sum = 0;
    for(int i = 0;i < a.length;i++){
        sum = sum * 10 + stoi(a);
    }
}
int V(int m,int n){
    if(m = 0){
        V(m,n) == n;
    }else if (n < m + 1)
    {
        cout << "run input" << endl;
        return 0;
    }else{
        
    }
    
}
int main(){
    int m;

}